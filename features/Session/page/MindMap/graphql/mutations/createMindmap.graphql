mutation CreateMindmap($input: ClientCreateMindMapDTO!) {
  createMindmap(input: $input) {
    createdAt
    data {
      edges {
        animated
        data {
          label
        }
        id
        source
        target
      }
    }
    id
    summary
    updatedAt
    workspaceId
  }
}
